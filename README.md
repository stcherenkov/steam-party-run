To override default configuration it is safe to create and use `env.config.yml` - it is not tracked by git.

To start application you need to run client-side and server-side by two different npm scripts.
